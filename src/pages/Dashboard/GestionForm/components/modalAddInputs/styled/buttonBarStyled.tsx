import styled from 'styled-components';

export const ButtonBarStyled = styled.div`
	align-items: flex-end;
	justify-content: flex-end;
	width: 100%;
	padding: 0.5rem 1rem;
	background-color: white;
	border-top: 1px solid var(--borderBoton);
	position: relative;
	bottom: 0;
	left: 0;
`;
